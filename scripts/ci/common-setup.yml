steps:
  - task: NodeTool@0
    displayName: Upgrade Node
    inputs:
      versionSpec: "20.x"

  - script: npm run check:package-quarantine
    displayName: Enforce package quarantine

  - script: npm ci
    displayName: Install dependencies
    retryCountOnTaskFailure: 3
